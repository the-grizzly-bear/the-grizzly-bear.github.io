# 2024-10-08 Base-p- (Miscellaneous)

Based.txt file

*[image unavailable]*

Where else is -p- used, but as syntax to scan all ports in nmap (i.e. base65536)

*[image unavailable]*

*[image unavailable]*

*[image unavailable]*

*[image unavailable]*

Get rid of the white background
trim to the transparent background
slice in 1pixel increments to obtain the web/hex color codes with the next command:

*[image unavailable]*

Filter through uniq, and the entries between the 00000000s are (as they appear from left to right) the color codes of each block:

*[image unavailable]*

*[image unavailable]*

```python
flag{586cf8c849c9730ea7b2112fff39ff6a}
```
